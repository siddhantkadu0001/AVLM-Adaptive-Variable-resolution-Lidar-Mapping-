# AVLM(Adaptive Variable-resolution Lidar Mapping)
Adaptive Variable-Resolution 2.5D Lidar Mapping for Dynamic Environment Perception — a deep learning pipeline that converts raw Lidar point clouds into a foveated, quadtree-based grid map (fine detail near the sensor, coarse detail far away) with real-time terrain and object classification.
